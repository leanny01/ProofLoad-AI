# Production only: backend API URL (e.g. from Render).
# Set this in Vercel env vars as VITE_API_URL.
# Leave empty/unset for local dev (Vite proxy handles /api).
# VITE_API_URL=https://your-backend.onrender.com
